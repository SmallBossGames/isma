# Task NN: <Title>

**Scope:** `<file path or module>`
**Effort:** `<Low / Medium / High>`
**Risk:** `<Very low / Low / Medium / High>`

## Problem Description

<What is wrong and why it needs changing. One short paragraph.>

## Expectations

<What the result should look like after this task is complete. What exists, what doesn't.>

## Implementation

### Key Design Decisions

- `<decision 1>`
- `<decision 2>`

### Implementation Steps

1. `<step 1>`
2. `<step 2>`

### High-Level Description

<Describe what the implementation should do, not how. Focus on behavior, data flow, and interactions. Subagents will handle the actual code writing.>

**Existing code:** `<which files/approach currently exist and what needs to change>`

**Target approach:** `<what the result should look like at a conceptual level — components, data flow, interfaces>`

## Tests

<What tests are needed. Unit tests, integration tests, edge cases. Specify which scenarios must be covered.>

### Test Scenarios

1. `<scenario 1 — e.g. happy path>`
2. `<scenario 2 — e.g. error handling>`
3. `<scenario 3 — e.g. edge cases>`

## Verification

- [ ] `<build command>` succeeds
- [ ] `<test command>` passes with full coverage
- [ ] `grep "<pattern>"` returns expected results

## Acceptance Criteria

- [ ] `<criterion 1>`
- [ ] `<criterion 2>`
- [ ] `<criterion 3>`
