package ru.nstu.isma.compiler.hsm.core;

import java.io.Serializable;

/** Marker for a compiled hybrid-system model. Domain contracts type against this, not the concrete HSM. */
public interface IHSM extends Serializable {
}
